import CourseLayout from "./index.js";

export const meta = {
  title: "Tuple Data Structure",
  metaTitle: "Python Tuple Data Structures - Use With Examples",
  description:
    "Tuples are one of the most impoartant data structure in Python. They are similar to lists in some sense but powerful then them.",
  date: "15 November 2020",
  course: "python",
  sequence: 4,
};

export default ({ children }) => (
  <CourseLayout meta={meta}>{children}</CourseLayout>
);

## This will be a tuple of python course.
